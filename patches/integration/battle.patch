diff --git a/src/battle_ai_script_commands.c b/src/battle_ai_script_commands.c
index 5cb0a60a6d6cdccf7f6278981a929e5f5ea84c5c..5b85e9b6090e0495cd4dcf7c310a0a9d151b6126 100644
--- a/src/battle_ai_script_commands.c
+++ b/src/battle_ai_script_commands.c
@@ -9,6 +9,7 @@
 #include "pokemon.h"
 #include "random.h"
 #include "recorded_battle.h"
+#include "reroll/reroll.h"
 #include "util.h"
 #include "constants/abilities.h"
 #include "constants/battle_ai.h"
@@ -298,9 +299,11 @@ void BattleAI_HandleItemUseBeforeAISetup(u8 defaultScoreMoves)
     {
         for (i = 0; i < MAX_TRAINER_ITEMS; i++)
         {
-            if (gTrainers[gTrainerBattleOpponent_A].items[i] != ITEM_NONE)
+            u16 item = Reroll_GetTrainerItem(gTrainerBattleOpponent_A, i);
+
+            if (item != ITEM_NONE)
             {
-                BATTLE_HISTORY->trainerItems[BATTLE_HISTORY->itemsNo] = gTrainers[gTrainerBattleOpponent_A].items[i];
+                BATTLE_HISTORY->trainerItems[BATTLE_HISTORY->itemsNo] = item;
                 BATTLE_HISTORY->itemsNo++;
             }
         }
@@ -371,9 +374,10 @@ void BattleAI_SetupAIData(u8 defaultScoreMoves)
     else if (gBattleTypeFlags & (BATTLE_TYPE_FRONTIER | BATTLE_TYPE_EREADER_TRAINER | BATTLE_TYPE_TRAINER_HILL | BATTLE_TYPE_SECRET_BASE))
         AI_THINKING_STRUCT->aiFlags = AI_SCRIPT_CHECK_BAD_MOVE | AI_SCRIPT_CHECK_VIABILITY | AI_SCRIPT_TRY_TO_FAINT;
     else if (gBattleTypeFlags & BATTLE_TYPE_TWO_OPPONENTS)
-        AI_THINKING_STRUCT->aiFlags = gTrainers[gTrainerBattleOpponent_A].aiFlags | gTrainers[gTrainerBattleOpponent_B].aiFlags;
+        AI_THINKING_STRUCT->aiFlags = Reroll_GetTrainerAiFlags(gTrainerBattleOpponent_A)
+                                    | Reroll_GetTrainerAiFlags(gTrainerBattleOpponent_B);
     else
-       AI_THINKING_STRUCT->aiFlags = gTrainers[gTrainerBattleOpponent_A].aiFlags;
+       AI_THINKING_STRUCT->aiFlags = Reroll_GetTrainerAiFlags(gTrainerBattleOpponent_A);
 
     if (gBattleTypeFlags & BATTLE_TYPE_DOUBLE)
         AI_THINKING_STRUCT->aiFlags |= AI_SCRIPT_DOUBLE_BATTLE; // act smart in doubles and don't attack your partner
diff --git a/src/battle_main.c b/src/battle_main.c
index 11fc168603f80a099134bc30e363dcc4c143d696..5e5ae64ac4ecd520e27d7ab1f791337f53b596df 100644
--- a/src/battle_main.c
+++ b/src/battle_main.c
@@ -36,6 +36,7 @@
 #include "pokemon.h"
 #include "random.h"
 #include "recorded_battle.h"
+#include "reroll/reroll.h"
 #include "roamer.h"
 #include "safari_zone.h"
 #include "scanline_effect.h"
@@ -694,6 +695,8 @@ static void CB2_InitBattleInternal(void)
 
     if (!(gBattleTypeFlags & (BATTLE_TYPE_LINK | BATTLE_TYPE_RECORDED)))
     {
+        if (!(gBattleTypeFlags & BATTLE_TYPE_WALLY_TUTORIAL))
+            Reroll_EnsurePlayerParty();
         CreateNPCTrainerParty(&gEnemyParty[0], gTrainerBattleOpponent_A, TRUE);
         if (gBattleTypeFlags & BATTLE_TYPE_TWO_OPPONENTS)
             CreateNPCTrainerParty(&gEnemyParty[PARTY_SIZE / 2], gTrainerBattleOpponent_B, FALSE);
@@ -1965,6 +1968,12 @@ static u8 CreateNPCTrainerParty(struct Pokemon *party, u16 trainerNum, bool8 fir
     s32 i, j;
     u8 monsCount;
 
+    // Ordinary story trainers are generated by the isolated Reroll engine.
+    if (trainerNum != TRAINER_SECRET_BASE
+     && gBattleTypeFlags & BATTLE_TYPE_TRAINER
+     && !(gBattleTypeFlags & (BATTLE_TYPE_FRONTIER | BATTLE_TYPE_EREADER_TRAINER | BATTLE_TYPE_TRAINER_HILL)))
+        return Reroll_CreateTrainerParty(party, trainerNum, firstTrainer);
+
     if (trainerNum == TRAINER_SECRET_BASE)
         return 0;
 
@@ -5216,6 +5225,8 @@ static void WaitForEvoSceneToFinish(void)
 
 static void ReturnFromBattleToOverworld(void)
 {
+    Reroll_OnBattleEnd();
+
     if (!(gBattleTypeFlags & BATTLE_TYPE_LINK))
     {
         RandomlyGivePartyPokerus(gPlayerParty);
diff --git a/src/battle_script_commands.c b/src/battle_script_commands.c
index 3af09ec7a281b99508f5e0170bb15f535d11df47..150bbaf3f79c2e3f85d041d50bed1058ba38fb88 100644
--- a/src/battle_script_commands.c
+++ b/src/battle_script_commands.c
@@ -3261,6 +3261,8 @@ static void Cmd_getexp(void)
     s32 viaExpShare = 0;
     u16 *exp = &gBattleStruct->expValue;
 
+    // Challenge parties are level-locked: skip experience and EV distribution.
+    gBattleScripting.getexpState = 6;
     gBattlerFainted = GetBattlerForBattleScript(gBattlescriptCurrInstr[1]);
     sentIn = gSentPokesToOpponent[(gBattlerFainted & 2) >> 1];
 
